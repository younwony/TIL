# Context Engineering (컨텍스트 엔지니어링)

> `[4] 심화` · 선수 지식: [Prompt Engineering](./prompt-engineering.md), [AI Agent란](./ai-agent.md)

> `Trend` 2026

> LLM이 작업을 성공적으로 수행하는 데 필요한 모든 정보(컨텍스트)를 동적으로 설계하고 관리하는 학문

`#ContextEngineering` `#컨텍스트엔지니어링` `#PromptEngineering` `#프롬프트엔지니어링` `#LLM` `#AIAgent` `#AI에이전트` `#Context` `#컨텍스트` `#ContextWindow` `#컨텍스트윈도우` `#Memory` `#메모리` `#RAG` `#Retrieval` `#검색` `#ToolOutput` `#도구출력` `#ConversationHistory` `#대화이력` `#SystemMessage` `#시스템메시지` `#DynamicContext` `#동적컨텍스트` `#ContextRot` `#컨텍스트부패` `#TokenOptimization` `#토큰최적화` `#MultiAgent` `#멀티에이전트` `#Anthropic` `#Claude`

## 왜 알아야 하는가?

2026년, AI 에이전트의 성공과 실패를 가르는 핵심 요소는 더 이상 모델의 성능이 아닙니다. **컨텍스트의 품질**입니다.

- **실무**: 대부분의 에이전트 실패는 모델 실패가 아닌 **컨텍스트 실패**에서 비롯됨
- **면접**: "Prompt Engineering과 Context Engineering의 차이는?", "에이전트가 실패하는 주요 원인은?"
- **기반 지식**: Multi-Agent 시스템, RAG, Memory 시스템 설계의 핵심 원리

## 핵심 개념

- **컨텍스트 = LLM이 추론 시점에 보는 모든 토큰**
- **동적 시스템**: 정적 프롬프트가 아닌, 상황에 따라 컨텍스트를 조립하는 시스템
- **최소 완전 정보**: AI가 작업에 필요한 최소한이지만 완전한 정보 제공
- **컨텍스트 부패(Context Rot)**: 오래된/불필요한 정보가 컨텍스트를 오염시키는 현상

## 쉽게 이해하기

**Prompt Engineering vs Context Engineering**을 요리사와 주방 총괄에 비유할 수 있습니다.

```
Prompt Engineering = 요리사 (레시피 작성)
Context Engineering = 주방 총괄 (전체 시스템 설계)
```

요리사(Prompt Engineer)는 맛있는 요리를 만드는 **레시피를 작성**합니다.

주방 총괄(Context Engineer)은:
- 어떤 재료를 언제 준비할지 (동적 정보 수집)
- 냉장고에서 어떤 재료를 꺼낼지 (메모리/RAG)
- 조리 도구를 언제 사용할지 (도구 출력)
- 손님 주문 이력을 어떻게 관리할지 (대화 이력)

**전체 주방 시스템을 설계**합니다.

## 상세 설명

### Prompt Engineering ⊂ Context Engineering

```
┌─────────────────────────────────────────────────────────────────┐
│                    Context Engineering                           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                                                            │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │          LLM 컨텍스트 윈도우                          │ │  │
│  │  │                                                       │ │  │
│  │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │ │  │
│  │  │  │   System    │  │    User     │  │ Conversation│  │ │  │
│  │  │  │   Message   │  │   Query     │  │   History   │  │ │  │
│  │  │  │  (지시사항)  │  │  (현재 요청) │  │  (대화 이력) │  │ │  │
│  │  │  └─────────────┘  └─────────────┘  └─────────────┘  │ │  │
│  │  │                                                       │ │  │
│  │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │ │  │
│  │  │  │   Long-term │  │  Retrieved  │  │    Tool     │  │ │  │
│  │  │  │   Memory    │  │  Documents  │  │   Outputs   │  │ │  │
│  │  │  │ (장기 기억)  │  │  (RAG 문서)  │  │  (도구 결과) │  │ │  │
│  │  │  └─────────────┘  └─────────────┘  └─────────────┘  │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │                                                            │  │
│  │  ┌──────────────────┐                                      │  │
│  │  │ Prompt Engineering│ ◄── 지시사항/프롬프트 작성에 집중   │  │
│  │  └──────────────────┘                                      │  │
│  │                                                            │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  Context Engineering = 전체 컨텍스트 조립 시스템 설계             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**Prompt Engineering**은 지시사항(instructions)을 어떻게 작성할지에 집중합니다.

**Context Engineering**은 LLM이 추론할 때 보게 될 **모든 정보의 조합**을 설계합니다.

### 핵심 차이점

| 구분 | Prompt Engineering | Context Engineering |
|------|-------------------|---------------------|
| **초점** | 무엇을 말할 것인가 | 모델이 무엇을 알아야 하는가 |
| **범위** | 지시문/프롬프트 | 전체 컨텍스트 윈도우 |
| **특성** | 정적, 텍스트 작성 | 동적, 시스템 아키텍처 |
| **스킬** | Writing skill | Architecture skill |
| **담당자** | 개발자 1명 | 데이터 엔지니어 + 도메인 전문가 + AI 전문가 협업 |

### 컨텍스트 구성 요소

```
┌─────────────────────────────────────────────────────────────────┐
│                    잘 설계된 컨텍스트                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. System Message (시스템 메시지)                               │
│     └── 역할, 제약조건, 출력 형식 지시                           │
│                                                                  │
│  2. User Query (사용자 질의)                                     │
│     └── 현재 요청/질문                                           │
│                                                                  │
│  3. Conversation History (대화 이력)                             │
│     └── 관련된 이전 대화만 선별                                  │
│                                                                  │
│  4. Long-term Memory (장기 기억)                                 │
│     └── 사용자 선호도, 이전 결정 사항                            │
│                                                                  │
│  5. Retrieved Documents (검색된 문서)                            │
│     └── RAG로 가져온 관련 정보                                   │
│                                                                  │
│  6. Tool Outputs (도구 출력)                                     │
│     └── API 호출, 데이터베이스 쿼리 결과                         │
│                                                                  │
│  7. Scratchpad (작업 메모)                                       │
│     └── 중간 추론 결과, 계획                                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**왜 이렇게 구성하는가?**

LLM은 컨텍스트 윈도우 안의 정보만으로 추론합니다. 필요한 정보가 없으면 환각(hallucination)이 발생하고, 불필요한 정보가 많으면 혼란을 일으킵니다.

### 컨텍스트 실패 유형

| 실패 유형 | 증상 | 원인 |
|----------|------|------|
| **정보 부족** | 환각, 부정확한 답변 | 필요한 정보가 컨텍스트에 없음 |
| **정보 과잉** | 무관한 응답, 지시 무시 | 불필요한 정보로 핵심이 묻힘 |
| **컨텍스트 부패** | 오래된 정보 기반 응답 | 대화 이력 관리 실패 |
| **형식 불일치** | 파싱 오류, 구조화 실패 | 도구 출력 형식 비일관 |

### 동적 컨텍스트 조립

```
┌─────────────────────────────────────────────────────────────────┐
│                   동적 컨텍스트 조립 파이프라인                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   사용자 요청                                                    │
│       │                                                          │
│       ▼                                                          │
│  ┌─────────────────┐                                            │
│  │  의도 분석      │                                            │
│  │  (Intent)       │                                            │
│  └────────┬────────┘                                            │
│           │                                                      │
│           ▼                                                      │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │              병렬 정보 수집                               │    │
│  │                                                          │    │
│  │  ┌───────┐  ┌───────┐  ┌───────┐  ┌───────┐            │    │
│  │  │ Memory │  │  RAG  │  │ Tools │  │History│            │    │
│  │  │ 조회   │  │ 검색  │  │ 실행  │  │ 필터  │            │    │
│  │  └───┬───┘  └───┬───┘  └───┬───┘  └───┬───┘            │    │
│  │      │          │          │          │                  │    │
│  └──────┴──────────┴──────────┴──────────┴──────────────────┘    │
│                              │                                    │
│                              ▼                                    │
│                    ┌─────────────────┐                           │
│                    │   컨텍스트 조립  │                           │
│                    │   & 최적화       │                           │
│                    └────────┬────────┘                           │
│                             │                                     │
│                             ▼                                     │
│                    ┌─────────────────┐                           │
│                    │    LLM 추론     │                           │
│                    └─────────────────┘                           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Multi-Agent 시스템에서의 중요성

Multi-Agent 시스템은 컨텍스트 요구사항을 기하급수적으로 증가시킵니다.

```
┌─────────────────────────────────────────────────────────────────┐
│              Multi-Agent 컨텍스트 폭발                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Single Agent:                                                   │
│  ┌──────────────────────────────────────────────┐               │
│  │ System + Query + History + RAG + Tools       │               │
│  └──────────────────────────────────────────────┘               │
│                                                                  │
│  Multi-Agent:                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ System + Query + History + RAG + Tools                    │   │
│  │ + Agent A 출력 + Agent B 출력 + Agent C 출력             │   │
│  │ + 에이전트 간 조정 메시지                                 │   │
│  │ + 작업 분배 계획                                          │   │
│  │ + 각 에이전트의 도구 정의                                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  문제: "Context Rot" - 컨텍스트가 오래된 정보로 오염됨           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**왜 Context Engineering이 필요한가?**

2026년에는 단순한 챗봇을 넘어 자율적으로 작동하는 Multi-Agent 시스템이 확산됩니다. 이때 "어떤 에이전트에게 어떤 컨텍스트를 얼마나 제공할 것인가"를 설계하는 것이 시스템 성공의 핵심입니다.

## 예제 코드

### 정적 프롬프트 vs 동적 컨텍스트

```python
# ❌ 정적 프롬프트 (Prompt Engineering만 적용)
prompt = """
당신은 고객 서비스 에이전트입니다.
고객의 질문에 친절하게 답변하세요.
"""

# ✅ 동적 컨텍스트 (Context Engineering 적용)
def build_context(user_query: str, user_id: str) -> str:
    # 1. 사용자 정보 조회
    user_profile = memory.get_user_profile(user_id)

    # 2. 관련 문서 검색 (RAG)
    relevant_docs = retriever.search(user_query, top_k=3)

    # 3. 최근 대화 이력 (최근 5개만)
    recent_history = history.get_recent(user_id, limit=5)

    # 4. 현재 주문 상태 조회 (도구 실행)
    order_status = tools.get_order_status(user_id)

    # 5. 컨텍스트 조립
    context = f"""
## 역할
당신은 고객 서비스 에이전트입니다.

## 고객 정보
- 이름: {user_profile.name}
- 등급: {user_profile.tier}
- 선호 언어: {user_profile.language}

## 현재 주문 상태
{order_status}

## 관련 FAQ
{format_docs(relevant_docs)}

## 최근 대화
{format_history(recent_history)}

## 현재 질문
{user_query}
"""
    return context
```

### 컨텍스트 최적화

```python
def optimize_context(context: str, max_tokens: int = 8000) -> str:
    """컨텍스트를 토큰 제한 내로 최적화"""

    current_tokens = count_tokens(context)

    if current_tokens <= max_tokens:
        return context

    # 우선순위에 따라 정보 제거
    # 1순위 보존: System Message, User Query
    # 2순위 보존: Tool Outputs, RAG Documents
    # 3순위 축소: Conversation History

    # 오래된 대화 이력부터 제거
    while current_tokens > max_tokens:
        context = trim_oldest_history(context)
        current_tokens = count_tokens(context)

    return context
```

### Claude Code에서의 Context Engineering

```typescript
// Claude Code의 내부 동작 예시 (개념적)
interface AgentContext {
  systemMessage: string;      // 에이전트 역할 정의
  userQuery: string;          // 현재 요청
  conversationHistory: Message[];  // 대화 이력
  workingDirectory: string;   // 현재 작업 디렉토리
  openFiles: FileContent[];   // 열려 있는 파일들
  toolDefinitions: Tool[];    // 사용 가능한 도구
  todoList: Todo[];           // 작업 목록
  previousToolOutputs: ToolOutput[];  // 이전 도구 실행 결과
}

function buildContext(request: Request): AgentContext {
  return {
    systemMessage: getSystemPrompt(),
    userQuery: request.message,
    conversationHistory: filterRelevantHistory(request.history),
    workingDirectory: process.cwd(),
    openFiles: getOpenFilesContent(),
    toolDefinitions: getAvailableTools(),
    todoList: getCurrentTodos(),
    previousToolOutputs: request.toolOutputs,
  };
}
```

## 트레이드오프

| 장점 | 단점 |
|------|------|
| 에이전트 신뢰성 향상 | 시스템 복잡도 증가 |
| 환각 감소 | 개발/유지보수 비용 |
| 일관된 응답 품질 | 지연 시간 증가 (정보 수집) |
| Multi-Agent 확장 가능 | 전문 인력 필요 |

## Best Practices

### 1. 최소 완전 정보 원칙

```
❌ 모든 정보를 다 넣기
✅ 작업에 필요한 최소한이지만 완전한 정보만 제공
```

### 2. 컨텍스트 위치 최적화

```
┌─────────────────────────────────────────────────────────────────┐
│                    컨텍스트 배치 전략                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [처음]  System Message (가장 중요한 지시사항)                   │
│    ↓                                                             │
│  [중간]  Reference Material (RAG 문서, 도구 출력)                │
│    ↓                                                             │
│  [마지막]  User Query + 핵심 지시 반복 ← 가장 잘 따름             │
│                                                                  │
│  💡 "Lost in the Middle" 현상 주의                               │
│     - LLM은 컨텍스트의 시작과 끝에 더 집중                        │
│     - 중요한 정보는 시작/끝에 배치                                │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 3. 정기적 컨텍스트 정리

```
❌ 모든 대화 이력 유지
✅ 주기적으로 요약하고 오래된 정보 제거
```

## 면접 예상 질문

### Q: Context Engineering과 Prompt Engineering의 차이점은 무엇인가요?

A: **Prompt Engineering**은 LLM에게 **무엇을 말할지**(지시문 작성)에 집중하는 반면, **Context Engineering**은 LLM이 추론 시점에 **무엇을 알아야 하는지**(전체 컨텍스트 설계)에 집중합니다. Prompt Engineering은 Context Engineering의 **부분집합**입니다. 예를 들어, System Prompt 작성은 Prompt Engineering이지만, RAG로 가져올 문서 선택, 대화 이력 관리, 도구 출력 포맷팅까지 포함하면 Context Engineering입니다.

### Q: 에이전트가 실패하는 가장 흔한 원인은 무엇인가요?

A: **컨텍스트 실패**입니다. 2026년 현재, 대부분의 에이전트 실패는 모델 성능 부족이 아니라 **잘못된 컨텍스트 설계**에서 비롯됩니다. 구체적으로: 1) 필요한 정보가 컨텍스트에 없거나, 2) 불필요한 정보가 너무 많거나, 3) 오래된 정보가 컨텍스트를 오염시키거나(Context Rot), 4) 도구 출력 형식이 일관되지 않은 경우입니다.

### Q: Multi-Agent 시스템에서 Context Engineering이 왜 더 중요해지나요?

A: Multi-Agent 시스템은 컨텍스트 요구사항을 **기하급수적으로 증가**시킵니다. 각 에이전트의 도구 정의, 에이전트 간 메시지, 작업 분배 계획 등이 추가되어 컨텍스트가 빠르게 증가합니다. 또한 **Context Rot** 문제가 심화됩니다. 따라서 "어떤 에이전트에게 어떤 정보를 얼마나 제공할지"를 체계적으로 설계하는 Context Engineering이 시스템 성공의 핵심이 됩니다.

### Q: "Lost in the Middle" 현상이란 무엇이며, 어떻게 대응하나요?

A: LLM은 컨텍스트의 **시작과 끝 부분에 더 집중**하고, 중간 부분의 정보를 놓치는 경향이 있습니다. 이를 "Lost in the Middle"이라고 합니다. 대응 방법: 1) 가장 중요한 지시사항을 **시작과 끝에 배치**, 2) 중간에는 참조용 정보(RAG 문서)를 배치, 3) 핵심 지시를 마지막에 **한 번 더 반복**, 4) 컨텍스트를 청크로 나누어 관리.

## 연관 문서

| 문서 | 연관성 | 난이도 |
|------|--------|--------|
| [Prompt Engineering](./prompt-engineering.md) | 선수 지식 (부분집합) | [3] 중급 |
| [RAG](./rag.md) | 검색된 문서 컨텍스트 | [3] 중급 |
| [Multi-Agent Systems](./multi-agent-systems.md) | 컨텍스트 복잡도 증가 | [4] 심화 |
| [AI Agent란](./ai-agent.md) | 에이전트 기초 개념 | [1] 정의 |
| [Tool Use](./tool-use.md) | 도구 출력 컨텍스트 | [2] 입문 |

## 참고 자료

- [Effective Context Engineering for AI Agents - Anthropic](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)
- [The New Skill in AI is Not Prompting, It's Context Engineering - Phil Schmid](https://www.philschmid.de/context-engineering)
- [Context Engineering Guide - Prompt Engineering Guide](https://www.promptingguide.ai/guides/context-engineering-guide)
- [Context Engineering: Going Beyond Prompt Engineering and RAG - The New Stack](https://thenewstack.io/context-engineering-going-beyond-prompt-engineering-and-rag/)
- [Beyond Prompt Engineering: The Shift to Context Engineering - NearForm](https://nearform.com/digital-community/beyond-prompt-engineering-the-shift-to-context-engineering/)
- [AI and Enterprise Technology Predictions for 2026 - Solutions Review](https://solutionsreview.com/ai-and-enterprise-technology-predictions-from-industry-experts-for-2026/)
